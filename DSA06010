#include<bits/stdc++.h>
using namespace std;

int main() {
int t;
cin>>t;
while(t--){
	int n;
	cin>>n;
	int a[n];
	set<char> b;
	for(int i=0; i<n; i++){
		cin>>a[i];
		string c=to_string(a[i]);
		for(int j=0; j<c.length();j++){
			b.insert(c[j]);
		}
	}
	for(auto x: b){
		cout<<x<<' ';
	}
	cout<<endl;
}
}
