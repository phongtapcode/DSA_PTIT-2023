#include <bits/stdc++.h>
using namespace std;


int main() {
	int t;
	cin >> t;
	while (t--) {
		string a;
		cin>>a;
		for(int i=1; i<a.length(); i++){
			if(a[i]!=a[i-1]){
				a[i]='1';
			}else{
				a[i]='0';
			}
		}
		cout<<a<<endl;
	}
	return 0;
}
